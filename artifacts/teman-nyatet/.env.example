# Supabase — required for auth only (notes/transactions/todos/links live in
# Google Sheets via the api-server, see artifacts/api-server/.env.example)
# Copy from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...your-anon-key

# Optional: override the public site URL used for email confirmation links.
# If omitted, the app falls back to the current page origin (works for
# Replit and Vercel previews automatically). Set this in production so
# Supabase confirmation emails always point to your canonical domain.
# VITE_SITE_URL=https://temannyatet.id

# API server — leave unset in Replit dev; the Vite dev server proxies /api to
# localhost:8080. Set this explicitly in the Vercel frontend project.
# Production also falls back to this same API URL if the variable is absent.
VITE_API_SERVER_URL=https://teman-nyatet-api-server.vercel.app

# Mayar payment page URL (frontend falls back to # if unset)
VITE_MAYAR_PAYMENT_URL=https://mayar.id/your-payment-page
