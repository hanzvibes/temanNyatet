{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && npx tsc --build lib/db && cd artifacts/api-server && pnpm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.mjs" }
  ]
}
